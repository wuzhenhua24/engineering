# Effective context engineering for AI agents

## 概述

**上下文工程（Context Engineering）**代表了从传统提示工程的转变——从制作单个提示转向管理推理期间语言模型可用的整个信息集。核心原则是将上下文视为"有限资源"，具有随着token积累而耗尽的"注意力预算"。

## 核心概念

### 上下文工程 vs 提示工程

**提示工程（Prompt Engineering）：**
- 专注于编写最佳指令
- 单次交互优化
- 静态指令集

**上下文工程（Context Engineering）：**
- 管理整个信息集
- 多轮交互优化
- 动态信息管理
- 跨越多个交互回合

### 关键区别

上下文工程不仅仅是编写好的提示，而是策划在推理过程中模型"看到"什么信息的整个过程。

## 技术细节

### 上下文腐化（Context Rot）

**定义：**
随着上下文长度增加，模型性能下降的现象。

**原因：**
1. **Transformer架构的n²配对token关系**
   - 随着上下文增长，模型捕获这些关系的能力受到压力

2. **训练数据偏差**
   - 模型在较长序列上的训练示例较少
   - 创建性能梯度而非悬崖

**影响：**
- 检索任务性能下降
- 长程推理能力减弱
- 注意力分散

### 最优高度原则（Optimal Altitude Principle）

系统提示应在特异性和灵活性之间取得平衡：

**避免两个极端：**
1. **过度工程化**：脆弱的if-else逻辑
2. **欠规范化**：模糊的高级指导

**目标：**
找到"恰到好处区域"（Goldilocks zone）：
- 足够具体以提供指导
- 足够灵活以适应不同场景
- 清晰但不僵化

## 实施方法

### 1. Just-In-Time上下文检索

**原理：**
- 维护轻量级标识符而非预加载所有数据
- 在执行期间动态检索信息

**实现方式：**

#### 数据库查询
- 按需查询数据库
- 只检索相关记录
- 避免加载整个数据集

#### Bash命令
使用工具如：
- `head`：查看文件开头
- `tail`：查看文件结尾
- 不加载完整对象

#### 文件系统导航
- 探索目录结构
- 按需读取文件
- 使用外部组织系统

**类比：**
模仿人类认知——使用外部组织系统而不是记忆所有内容。

**Claude Code示例：**
代理查询数据库、使用bash命令、导航文件系统，而无需加载完整对象。

### 2. 工具设计优化

**设计原则：**

#### 自包含且明确
- 每个工具有清晰的单一目的
- 接口明确定义
- 文档完整

#### 最小重叠
- 工具功能不应重复
- 清晰的职责划分
- 避免混淆

**关键原则：**
> "如果人类工程师无法明确说出在给定情况下应该使用哪个工具，那么AI代理也无法做得更好。"

#### Token效率
- 返回高信号信息
- 使用人类可读的名称而非神秘标识符
- 避免冗余输出

#### 清晰的用途说明
- 明确工具的预期用途
- 提供使用示例
- 说明限制和约束

### 3. 结构化示例

**Few-Shot Prompting优势：**
- 使用多样化、规范的示例
- 而非详尽的边缘情况列表

**重要观点：**
> "示例对LLM来说是价值千言的图片"

**最佳实践：**
- 选择代表性示例
- 覆盖主要用例
- 展示预期格式和风格

## 长期任务技术

### 1. Compaction（压缩）

**定义：**
当接近上下文限制时总结和压缩对话历史。

**实施策略：**

#### 保留关键信息
- 架构决策
- 重要细节
- 当前状态

#### 丢弃冗余内容
- 重复的工具输出
- 临时计算
- 已完成的子任务

#### 优化流程
1. **初期**：最大化召回率（保留更多）
2. **后期**：提高精确度（更精简）

### 2. 结构化笔记（Structured Note-Taking）

**机制：**
代理维护上下文窗口外持久化的外部内存文件。

**文件类型：**
- `CLAUDE.md`：主要笔记
- `NOTES.md`：详细记录
- To-do lists：任务追踪

**实例：Claude玩Pokemon**
- 维护数千步骤的精确统计
- 实现多小时策略
- 持久化关键信息

**优势：**
- 突破上下文窗口限制
- 保持长期一致性
- 支持复杂策略

### 3. 子代理架构（Sub-Agent Architectures）

**架构模式：**

#### 主代理（Main Agent）
- 协调整体任务
- 分配子任务
- 综合结果

#### 子代理（Sub-Agents）
- 处理专注任务
- 清洁的上下文窗口
- 广泛探索

**工作流程：**
```
主代理
  ├─→ 子代理1（专注任务A）
  ├─→ 子代理2（专注任务B）
  └─→ 子代理3（专注任务C）
         ↓
  返回压缩摘要（1,000-2,000 tokens）
```

**优势：**
- 隔离的上下文
- 并行处理
- 压缩结果返回

## 主要优势

### 1. 改进的连贯性
- 代理保持对相关信息子集的关注
- 减少注意力分散
- 提高任务完成质量

### 2. 扩展的能力
- 通过压缩和内存实现长期任务
- 突破单一上下文窗口限制
- 支持复杂策略

### 3. 成本效率
- Token高效检索减少计算开销
- 避免不必要的信息加载
- 优化资源使用

### 4. 灵活的自主性
- 渐进式披露允许代理逐步发现上下文
- 适应性强
- 支持探索性任务

## 挑战讨论

### 1. 权衡复杂性

**运行时探索成本：**
- 比预计算检索成本更高
- 需要权衡速度与灵活性
- 适当场景选择

### 2. 压缩精度

**风险：**
- 激进的总结可能丢失后来变得重要的细微上下文
- 需要平衡压缩率和信息保留

**缓解策略：**
- 保守的初始压缩
- 多层压缩策略
- 关键信息标记

### 3. 工具歧义

**问题：**
- 臃肿的工具集造成最佳工具选择困惑
- 重叠功能导致决策困难

**解决方案：**
- 清晰的工具职责划分
- 详细的使用文档
- 定期工具审查和整合

### 4. 上下文污染

**现象：**
- 即使更大的上下文窗口也会面临信息相关性问题
- 不是所有信息都同等重要

**应对：**
- 智能过滤
- 相关性评分
- 动态调整

## 重要结论

### 核心原则

**指导原则：**
> "找到最小的高信号token集，最大化某些期待结果的可能性。"

### 最佳实践

**1. 做最简单有效的事情**
- 避免过度复杂化
- 从简单开始
- 逐步优化

**2. "Just in Time"策略**
- 随着模型改进，这一领域正朝着"即时"策略发展
- 按需加载信息
- 动态调整策略

**3. 将上下文视为珍贵且有限**
- 不要浪费token
- 精心策划信息
- 持续优化

### 未来趋势

**模型进化：**
- 随着模型变得更强大，需要更少的规范性工程
- 实现更大的自主性
- 但上下文管理仍然重要

**持续相关性：**
- 即使未来上下文窗口扩大
- 将上下文视为珍贵和有限仍将是有效代理开发的核心

## 实施建议

### 初学者

1. **从简单开始**
   - 实施基本的just-in-time检索
   - 使用清晰的工具定义
   - 避免复杂架构

2. **逐步优化**
   - 监控性能
   - 识别瓶颈
   - 有针对性地改进

### 中级

1. **实施压缩**
   - 当达到上下文限制时
   - 保留关键信息
   - 测试不同策略

2. **结构化笔记**
   - 建立外部内存系统
   - 标准化格式
   - 定期更新

### 高级

1. **子代理架构**
   - 复杂任务分解
   - 并行处理
   - 结果综合

2. **混合策略**
   - 结合多种技术
   - 根据任务调整
   - 持续监控优化

## 评估指标

### Token使用效率
- 每个任务的平均token使用
- 成功率与token使用的关系
- 浪费的token比例

### 任务完成质量
- 准确性
- 完整性
- 一致性

### 系统性能
- 响应时间
- 成本效率
- 可扩展性

## 工具设计检查清单

- [ ] 工具目的清晰明确
- [ ] 文档完整详细
- [ ] 返回token高效
- [ ] 使用人类可读标识符
- [ ] 功能不重叠
- [ ] 包含使用示例
- [ ] 错误处理清晰
- [ ] 性能优化

## 关键要点

1. **上下文是有限资源**：需要精心管理
2. **Just-in-time检索**：按需加载信息
3. **工具设计至关重要**：清晰、高效、无歧义
4. **长期任务需要特殊技术**：压缩、笔记、子代理
5. **简单优先**：做最简单有效的事情
6. **持续优化**：基于实际性能迭代改进

## 结论

有效的上下文工程是构建高性能AI代理的关键。通过将上下文视为珍贵的有限资源，采用just-in-time检索、优化工具设计和适当的长期任务技术，开发者可以显著提高代理的能力和效率。

随着模型不断改进，上下文工程的具体技术可能会演变，但核心原则——精心策划和高效使用上下文——将继续是有效代理开发的基础。

---

*原文链接：https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents*
