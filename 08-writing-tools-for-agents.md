# Writing effective tools for agents — with agents

## 概述

本文探讨软件开发的一个基本转变：为非确定性AI代理设计工具，而不是传统的确定性系统。正如文章所述，"工具是一种新型软件，反映了确定性系统和非确定性代理之间的契约。"

核心见解：代理对工具的感知和使用方式与传统软件不同。它们有限的上下文窗口和必须策略性地导航众多工具的需求，使得符合人体工程学的设计至关重要。

## 核心概念

### 代理工具的独特性

**传统API vs 代理工具：**

| 传统API | 代理工具 |
|---------|---------|
| 确定性调用 | 非确定性选择 |
| 程序化访问 | 自然语言理解 |
| 完整文档可查 | 上下文窗口有限 |
| 精确参数 | 意图推断 |

### 代理视角

关键问题：
> "把自己放在模型的位置。基于描述和参数，如何使用这个工具是否明显？"

## 工具开发流程

### 三阶段方法论

#### 1. 原型设计（Prototyping）

**目标：**
快速构建工具以验证概念

**方法：**
- 使用Claude Code快速开发
- 使用适当的文档（如`llms.txt`文件）
- 通过本地MCP服务器或Desktop扩展连接测试

**关键活动：**
- 快速迭代
- 基本功能验证
- 初步接口设计

#### 2. 评估（Evaluation）

**目标：**
系统化测试工具性能

**方法：**
- 创建需要多个工具调用的现实评估任务
- 运行带有简单代理循环的程序化评估
- 收集关键指标

**收集的指标：**
- **准确性**：任务完成正确性
- **运行时**：执行时间
- **Token消耗**：上下文使用效率
- **错误**：失败类型和频率

**最佳实践：**
- 使用代表性任务
- 覆盖常见和边缘情况
- 自动化评估流程

#### 3. 优化（Optimization）

**目标：**
基于评估结果改进工具

**方法：**
- 使用Claude分析交互记录
- 重构工具设计
- 使用保留测试集防止过拟合

**策略：**
- 识别失败模式
- 优化工具描述
- 简化接口
- 改进错误处理

## 实施方法

### 工具选择策略

#### 优先高影响工作流

**错误方法：**
包装每个API端点

**正确方法：**
优先考虑高影响工作流

**地址簿类比：**
- ❌ 不要实现：`list_contacts`（返回所有联系人）
- ✅ 应该实现：`search_contacts`（仅返回相关联系人）

**原因：**
避免在不相关数据上浪费上下文

### 工具整合原则

#### 捆绑相关操作

**示例1：事件调度**
```
整合前：
- list_users
- list_events
- create_event

整合后：
- schedule_event（组合所有操作）
```

**示例2：日志搜索**
```
整合前：
- get_all_logs（返回全部）

整合后：
- search_logs（过滤返回）
```

**示例3：客户上下文**
```
整合前：
- get_transactions
- get_notes

整合后：
- get_customer_context（组合相关信息）
```

**优势：**
- 减少工具数量
- 降低上下文使用
- 简化代理决策

### 命名空间组织

#### 清晰的前缀/后缀

**目的：**
帮助代理选择适当的工具

**示例：**
```
asana_search
asana_projects_search
asana_tasks_search
```

**影响：**
文章指出基于前缀和后缀的命名空间对评估性能有"非微不足道的影响"

### 响应格式控制

#### 枚举参数模式

**实现：**
```typescript
enum ResponseFormat {
   DETAILED = "detailed",
   CONCISE = "concise"
}
```

**优势：**
- 代理可以请求简洁或详细响应
- 平衡token效率与必要上下文
- 根据需求动态调整

**实例：**
从206 token减少到72 token，同时保持功能性

## 有效工具的原则

### 1. 清晰的目的

**要求：**
- 每个工具应有明确的角色
- 不重叠的功能
- 清晰的使用场景

**验证：**
如果人类无法明确说出何时使用某个工具，代理也不能

### 2. 有意义的上下文

**返回高信号信息：**
- 使用人类可读的名称
- 避免神秘标识符
- 提供相关上下文

**反例：**
- 技术UUID使代理困惑
- 无意义的内部ID

**正例：**
- 语义标识符
- 描述性名称
- 上下文相关信息

### 3. Token优化

**实施技术：**

#### 分页
- 返回部分结果
- 支持迭代访问
- 避免一次性大量数据

#### 过滤
- 服务器端过滤
- 只返回相关数据
- 减少不必要传输

#### 截断
- 合理的默认值
- 可配置限制
- 清晰的截断指示

### 4. 提示工程

**工具描述的重要性：**
- 精心制作工具描述
- 即使小的改进也能产生"显著改进"
- 包含使用示例

**实例：**
Claude不必要地在搜索查询后附加"2025"，直到工具描述被优化。

### 5. 响应结构

**格式选择：**
- XML
- JSON
- Markdown

**注意：**
没有通用解决方案，基于评估结果选择

## 代码示例

### ResponseFormat枚举

```typescript
enum ResponseFormat {
   DETAILED = "detailed",
   CONCISE = "concise"
}

function getCustomerInfo(
  customerId: string,
  format: ResponseFormat
): string {
  const data = fetchCustomerData(customerId);

  if (format === ResponseFormat.CONCISE) {
    return summarize(data);  // 72 tokens
  } else {
    return data;  // 206 tokens
  }
}
```

### 工具整合示例

```typescript
// 整合前
function listUsers() { /* ... */ }
function listEvents() { /* ... */ }
function createEvent() { /* ... */ }

// 整合后
function scheduleEvent(params: {
  participants: string[];
  title: string;
  datetime: string;
}) {
  // 内部处理所有步骤
  const users = filterRelevantUsers(params.participants);
  const conflicts = checkAvailability(users, params.datetime);
  return createEventIfNoConflicts(params, conflicts);
}
```

## 主要优势

### 1. 上下文效率

**成果：**
- 周到的工具减少在不相关信息上浪费的上下文
- 显著降低token使用
- 更高效的代理操作

### 2. 改进的准确性

**原因：**
- 清晰的工具描述
- 整合减少选择错误
- 明确的使用场景

### 3. 可测量的性能

**方法：**
- 系统化评估揭示瓶颈
- 识别改进机会
- 量化优化效果

### 4. 迭代改进

**过程：**
- 代理可以帮助优化自己的工具
- 通过分析发现问题
- 持续改进循环

**验证：**
Claude优化的工具在Slack和Asana服务器上优于专家人类编写的实现

## 挑战讨论

### 1. 预测代理行为

**问题：**
对代理符合人体工程学的工具并不总是直观可预测的

**解决：**
- 需要实际测试
- 观察实际使用
- 迭代改进

### 2. 上下文限制

**问题：**
代理无法像传统软件那样有效处理大量结果集

**解决：**
- 实施过滤和分页
- 返回高信号数据
- 避免信息过载

### 3. 工具扩散

**问题：**
过多重叠工具分散代理注意力

**解决：**
- 整合相关功能
- 清晰的职责划分
- 定期审查和精简

### 4. 标识符混淆

**问题：**
技术UUID使代理困惑

**解决：**
- 使用语义标识符
- 提供描述性名称
- 包含上下文信息

### 5. 错误沟通

**问题：**
不透明的错误代码阻止代理自我纠正

**解决：**
- 清晰的错误消息
- 可操作的错误信息
- 包含修复建议

## 评估驱动开发

### 核心理念

**不要假设，要测量：**
- 系统化测量代理性能
- 基于数据做决策
- 持续验证改进

### 协作优化

**重要发现：**
> "代理是你发现问题的有益伙伴"

**方法：**
- 让代理参与工具改进
- 分析代理行为模式
- 利用代理洞察

### 人类直觉性

**意外发现：**
为代理人体工程学优化的工具对人类来说也"出奇地直观"

**启示：**
- 代理友好的设计往往也是人类友好的
- 清晰性对两者都有益
- 好的工具设计是通用的

## 最佳实践总结

### 设计阶段

1. **从高影响工作流开始**
   - 识别最常见任务
   - 优先核心功能
   - 避免过度工程化

2. **整合相关操作**
   - 组合相关功能
   - 减少工具数量
   - 简化代理决策

3. **清晰的命名和组织**
   - 使用描述性名称
   - 实施命名空间
   - 保持一致性

### 实施阶段

1. **优化返回数据**
   - 实施过滤
   - 添加分页
   - 支持截断

2. **详细的文档**
   - 清晰的工具描述
   - 包含示例
   - 说明边缘情况

3. **灵活的响应格式**
   - 支持简洁/详细模式
   - 可配置输出
   - 根据需求调整

### 测试阶段

1. **创建评估任务**
   - 覆盖常见场景
   - 包含边缘情况
   - 自动化测试

2. **收集指标**
   - 准确性
   - Token使用
   - 错误率
   - 执行时间

3. **迭代优化**
   - 分析失败
   - 优化工具
   - 重新评估

## 未来演进

### 模型进化

**随着代理和LLM演进：**
- 需要适应新能力
- 持续优化工具
- 保持评估更新

### 系统化方法的价值

**长期优势：**
- 确保工具保持有效
- 适应变化的能力
- 持续改进文化

## 实用建议

### 开始时

1. **快速原型**
   - 使用Claude Code
   - 本地测试
   - 快速迭代

2. **早期测试**
   - 创建基本评估
   - 收集初步数据
   - 识别明显问题

3. **逐步优化**
   - 一次改进一个方面
   - 测量每个变化
   - 保持改进记录

### 扩展时

1. **标准化流程**
   - 建立评估框架
   - 自动化测试
   - 文档化最佳实践

2. **团队协作**
   - 分享学习成果
   - 共同改进
   - 建立工具库

3. **持续监控**
   - 跟踪生产使用
   - 收集反馈
   - 定期审查

## 关键要点

1. **评估驱动**：系统化测量而非假设
2. **代理协作**：利用代理帮助优化工具
3. **上下文优先**：设计时考虑上下文效率
4. **整合优于扩散**：捆绑相关功能
5. **清晰沟通**：工具描述至关重要
6. **迭代改进**：持续测试和优化
7. **认知兼容**：为语言模型的思维方式设计

## 范式转变

### 从传统API设计到认知兼容性

**传统方法：**
- 完整的端点覆盖
- 技术导向
- 确定性假设

**代理优先方法：**
- 高影响工作流
- 认知导向
- 非确定性设计

**结果：**
这代表了从传统API设计向与语言模型认知兼容性的范式转变。

## 结论

为AI代理编写有效工具需要新的思维方式。通过采用评估驱动的开发方法，优先考虑代理的认知限制，并与代理协作优化工具，开发者可以创建显著提高代理性能的工具。

关键是要记住：工具不仅要在功能上正确，还要在认知上兼容。这种双重关注使得工具既对代理有效，也对人类开发者直观——实现了两全其美。

---

*原文链接：https://www.anthropic.com/engineering/writing-tools-for-agents*
