# Beyond permission prompts: Making Claude Code more secure and autonomous

## 概述

Anthropic引入了Claude Code的沙箱功能，旨在增强安全性和自主操作能力。核心思想是解决一个基本矛盾：让Claude访问代码库会带来风险（特别是提示注入攻击），但要求持续的权限批准会造成"批准疲劳"并减慢开发速度。

## 核心概念

### 安全与自主性的平衡

在定义的边界内，Claude可以自主执行命令而无需权限提示。尝试访问沙箱外的资源会立即通知用户。

### 双边界架构

沙箱方法实现了双重隔离机制：

#### 1. 文件系统隔离
- Claude只能访问或修改特定目录
- 防止代理被攻破时修改系统文件
- 基于OS级别的原语实现

#### 2. 网络隔离
- Claude仅通过代理连接到批准的服务器
- 阻止未经授权的数据泄露或恶意软件下载
- 涵盖直接操作和生成的子进程

## 技术实现

### OS级别原语

实现利用了操作系统级别的安全机制：
- **Linux**：bubblewrap
- **macOS**：seatbelt

这些机制在内核级别强制执行限制，涵盖：
- Claude Code的直接操作
- 所有生成的子进程

### Sandboxed Bash工具

引入了新的沙箱运行时（作为beta研究预览版和开源代码提供）：

**功能特性：**
- 用户定义可访问目录
- 用户定义可访问网络主机
- 在定义边界内自主执行
- 尝试越界时立即通知用户

### Claude Code on the Web

云端部署实现了更完善的隔离：

**架构特点：**
- 每个会话隔离
- 敏感凭证（git token、签名密钥）保留在沙箱外
- 自定义代理服务透明处理git交互
- 验证凭证并确保只推送到配置的分支

## 实施效果

### 量化改进

内部测试结果显示：
- **权限提示减少84%**
- 同时保持安全性
- 消除批准疲劳
- 加快开发周期

### 安全增强

- 通过架构隔离防止提示注入攻击
- 保护SSH密钥不被泄露
- 防止沙箱逃逸

## 挑战与缓解措施

### 双重防护的必要性

文章强调两个边界都是必需的：

1. **仅网络隔离的风险**：可能导致SSH密钥泄露
2. **仅文件系统隔离的风险**：无法防止沙箱逃逸

**结论**：两个组件必须协同工作才能提供有效保护

### 实际考虑

- 需要适当配置可访问目录和主机
- 需要平衡安全性和功能性
- 需要监控和审计能力

## 使用建议

### 如何启用沙箱

开发者可以通过在Claude Code中运行 `/sandbox` 命令来激活沙箱功能。

### 最佳实践

1. **明确定义边界**
   - 仔细选择可访问的目录
   - 只允许必要的网络主机

2. **定期审查**
   - 检查沙箱配置
   - 监控访问尝试

3. **集成到工作流**
   - 在开发周期早期启用
   - 作为标准安全实践

## 开源贡献

### 社区参与

- 沙箱运行时已开源
- 鼓励集成到其他代理系统
- 提升整个AI生态系统的安全态势

### 技术共享

开源组件包括：
- 沙箱实现代码
- 配置示例
- 集成指南

## 关键优势总结

1. **减少摩擦**：84%减少权限提示
2. **保持安全**：双边界架构保护
3. **提升效率**：更快的开发周期
4. **防御攻击**：抵御提示注入
5. **开源共享**：促进生态系统安全

## 技术创新点

### 架构设计
- 双边界隔离机制
- OS级别安全原语
- 透明代理服务

### 用户体验
- 自主操作与安全平衡
- 即时越界通知
- 简化的配置流程

## 未来展望

- 持续改进沙箱机制
- 扩展到更多使用场景
- 社区驱动的功能增强
- 与其他安全工具集成

## 结论

Claude Code的沙箱功能代表了在AI代理安全性和自主性之间取得平衡的重要进步。通过双边界架构和OS级别的安全机制，系统在保持高度安全性的同时显著提升了开发效率。开源的实现方式鼓励了更广泛的采用和改进，有助于提升整个AI生态系统的安全水平。

---

*原文链接：https://www.anthropic.com/engineering/claude-code-sandboxing*
