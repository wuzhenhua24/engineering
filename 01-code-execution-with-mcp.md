# Code execution with MCP: Building more efficient agents

## 概述

本文探讨如何通过代码执行增强模型上下文协议（Model Context Protocol, MCP），创建更高效的AI代理。核心观点是：代理应该编写代码来与MCP服务器交互，而不是直接暴露工具定义和结果给语言模型——这类似于开发人员编写程序的方式。

## 核心问题

### 1. 工具定义过载（Tool Definition Overload）

当代理连接到数百或数千个MCP工具时，预先加载所有定义会消耗大量上下文。连接到众多工具的代理"在读取请求之前需要处理数十万个token"。

### 2. 中间结果膨胀（Intermediate Result Bloat）

工具调用之间流动的数据会多次通过模型。文章举例：一个会议记录在检索时通过上下文一次，写入另一个系统时又通过一次——可能消耗额外的50,000+ token。

## 解决方案：代码执行方式

### 实现方法

将MCP工具呈现为通过文件系统结构访问的代码API：

```
servers/
├── google-drive/
│   ├── getDocument.ts
│   └── index.ts
└── salesforce/
    ├── updateRecord.ts
    └── index.ts
```

代理通过探索文件系统发现工具，只读取当前任务需要的定义。这实现了"渐进式披露"（progressive disclosure）——按需加载工具定义，而不是预先加载。

## 关键优势

### Token效率
- **显著降低使用量**：案例显示从150,000 token减少到2,000 token——减少98.7%

### 数据处理能力
- **智能过滤**：大型数据集可以在执行环境中过滤，只将相关结果传递给模型
- **实例**：不是将10,000行电子表格数据通过上下文传递，代理可以处理并只返回相关行

### 隐私保护
- **中间结果隔离**：默认情况下，中间结果保留在执行环境中
- **自动PII标记化**：敏感数据永远不会进入模型，而是直接在工具之间流动

### 状态持久化
- **保存进度**：代理可以将进度保存到文件
- **可复用技能**：开发可在未来使用的代码技能，随时间构建越来越复杂的能力

### 改进的控制流
- **熟悉的编程结构**：循环和条件等构造在代码中执行，而不需要顺序的模型调用

## 挑战与权衡

### 基础设施需求

文章指出重要的实现成本：

> "运行代理生成的代码需要具有适当沙箱、资源限制和监控的安全执行环境。"

这些基础设施需求增加了直接工具调用所没有的运营开销，需要仔细评估收益与成本。

## 技术细节

### 渐进式工具发现
- 代理探索文件系统以发现可用工具
- 只加载当前任务相关的工具定义
- 避免上下文窗口污染

### 执行环境特性
- 安全沙箱
- 资源限制
- 监控能力
- 文件系统访问

## 结论

代码执行通过应用成熟的软件工程模式，改变了代理与MCP服务器的交互方式。这种方法使代理能够：

1. 处理更多工具
2. 减少token消耗
3. 降低延迟
4. 减少运营成本

虽然实现复杂性必须仔细权衡这些优势，但这代表了构建更高效AI代理的重要方向。

## 关键要点

- **架构转变**：从直接工具调用到代码执行接口
- **效率提升**：98.7%的token使用量减少
- **安全考虑**：需要适当的沙箱和监控
- **适用场景**：特别适合需要处理大量工具和数据的复杂任务

---

*原文链接：https://www.anthropic.com/engineering/code-execution-with-mcp*
